#!/usr/bin/env ruby

require 'learn'

COMMANDS = [
  'test'
]

# Default to running tests
if ARGV.empty? || ARGV.none? {|arg| COMMANDS.include?(arg)}
  ARGV.unshift('test')
end

Learn::CLI.start(ARGV)
